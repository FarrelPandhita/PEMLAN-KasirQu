# Database Migrations

This folder contains incremental database changes.
DO NOT edit `masterDB.sql` directly for iterative changes.
Instead, create a new SQL file here (e.g., `V2_add_nama_operator.sql`).
